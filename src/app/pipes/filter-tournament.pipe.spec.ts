import { FilterTournamentPipe } from './filter-tournament.pipe';

describe('FilterTournamentPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTournamentPipe();
    expect(pipe).toBeTruthy();
  });
});
